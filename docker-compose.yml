services:
  app:
    build: .
    container_name: trailing-stop
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      - DATA_DIR=/app/data
    volumes:
      - trailing-stop-data:/app/data

volumes:
  trailing-stop-data:
